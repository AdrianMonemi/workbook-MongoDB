import React, {useEffect,useState} from "react";
import "./Button.css";

function Button({}) {

  return(
    <div>
      <button type="submit" onClick={}></button>
    </div>
  );
}

export default Button;